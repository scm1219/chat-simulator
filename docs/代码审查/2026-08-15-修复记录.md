# 2026-08-15 代码审查修复记录

| 项目 | 内容 |
|---|---|
| 修复分支 | `fix/code-review-2026-08-15` |
| 修复范围 | 审查报告全部 S/H/M 编号 + 低优先级表逐行；对应 36 个修复任务（T1–T36） |
| Commit 范围 | `e2f0ebe..1ab2f7e`（计划文档 commit 之后共 37 个修复/测试 commit） |
| 验证结果 | `pnpm lint` 0 错误；`pnpm build` 成功（main/preload/renderer 全部产出）；`pnpm test` 36 pass / 0 fail |
| 状态口径 | 已修复（附 commit 短哈希）/ 遗留（P3 或规格 §4 排除项）/ 复查不成立（本计划执行中无此项） |

测试覆盖（node:test，8 个文件 36 例）：html-escape、timestamp、like-escape、context-truncate、json-extractor、logger、statement-cache、stream-batcher、narrative-parsing。

---

## 严重问题（S）

| 编号 | 状态 | 修复 commit | 备注 |
|---|---|---|---|
| S-1 | 已修复 | a244f1a | syncGroupsProfile 改用合并后完整 profile 同步，四字段统一兜底（T7） |
| S-2 | 已修复 | 9032acf | 先 HTML 实体转义再高亮；escapeHtml 工具与测试来自 a826f96（T1/T2） |

## 高优先级问题（H）

| 编号 | 状态 | 修复 commit | 备注 |
|---|---|---|---|
| H-1 | 已修复 | 61d0845 | groupId 白名单正则校验 + 路径穿越防护（assertPathInsideDataDir）（T4） |
| H-2 | 已修复 | 02ca48c | API Key 经 safeStorage 加密存储，读取兼容旧明文迁移（T5） |
| H-3 | 已修复 | 1a844ab | index.html CSP meta、setWindowOpenHandler deny、will-navigate 拦截、sandbox: true（T3） |
| H-4 | 已修复 | c12e2f4 | budget_tokens 随 maxTokens 动态计算且小于 max_tokens（T14） |
| H-5 | 已修复 | 49a0c72 | 引入 socks-proxy-agent@8，socks 协议时设置 httpAgent/httpsAgent 并 proxy:false（T13） |
| H-6 | 已修复 | f88e566 | timestamp 毫秒化（字典序兼容秒级存量）+ 删除改按 rowid 定位（T8） |
| H-7 | 已修复 | 72947d9 | Map<groupId, AbortController> 防重入与取消 + safeSend 封装（T23） |
| H-8 | 已修复 | ed2e70c | 导出 ZIP 改 copyFile+unlink，规避 EXDEV（T26） |
| H-9 | 已修复 | 50bca16 | json-extractor 改 ESM 具名导出，附 ESM 导入测试（T29） |
| H-10 | 已修复 | 50bca16 | rename 移入 stream.end() 回调 + _cleanOldLogs 兼容 .old.log（T29） |
| H-11 | 已修复 | 25d6361 | useVirtualizer 增加 getItemKey(messages[i].id)（T20） |
| H-12 | 已修复 | 4499a80 | 流式/新消息/余波事件统一 groupId 守卫（T19） |
| H-13 | 已修复 | 4499a80 | loadMessages/loadCharacters 请求序号防护（T19） |
| H-14 | 已修复 | 4499a80 | 防重复注册改为 messageListener?.()，暴露统一 cleanup（T19） |
| H-15 | 已修复 | b3aa197 | 凭据预填按 providerId 过滤，未命中 Key 留空（T32） |
| H-16 | 已修复 | b3aa197 | thinking 字段统一驼峰 thinkingEnabled，读取处兼容旧下划线（T32） |

## 中优先级问题（M）

| 编号 | 状态 | 修复 commit | 备注 |
|---|---|---|---|
| M-1 | 已修复 | 832e8d5 | getSystemPromptTemplates/reset 全部深拷贝返回（T11） |
| M-2 | 已修复 | a5eba53 | setCharacterTags 包 db.transaction（T10） |
| M-3 | 已修复 | c9d2c3b | 配置文件 temp+rename 原子写 + enqueueWrite 串行队列（T9） |
| M-4 | 已修复 | 4750bd4 | 最小方案：全局角色改名时迁移记忆关联；schema 改 character_id 关联为遗留项（T31） |
| M-5 | 已修复 | 832e8d5 | GROUP_COLUMNS 补 4 列（T11） |
| M-6 | 已修复 | 832e8d5 | 主进程 maxHistory 边界（0 合法/负数 NaN 回退 20，修正于 9fca7de）；前端 1-50 校验回显于 1ab2f7e（T11/T36） |
| M-7 | 已修复 | 6a4807c | Ollama 全链路接入 signal，catch 增加取消分支（T16） |
| M-8 | 已修复 | 3302a8c | 错误体流式读取 + destroy 释放（T15） |
| M-9 | 已修复 | 3302a8c | data 回调 try-catch 防护，异常时清理流与监听器（T15） |
| M-10 | 已修复 | 3302a8c | [DONE]/message_stop 后 destroy 流（T15） |
| M-11 | 已修复 | c12e2f4 | SSE data 载荷 type 字段优先 + currentEvent 逐事件重置（T14） |
| M-12 | 已修复 | 6a4807c | Ollama 流式末帧 usage 统计不再丢弃（T16） |
| M-13 | 已修复 | 6a4807c | streamEnabled 配置真实生效（T16） |
| M-14 | 已修复 | 95c9894 | createHandler 统一错误日志 + message 脱敏（去绝对路径/SQL 片段）（T6） |
| M-15 | 已修复 | ed2e70c | 输出流 error 监听 + finalize await；生成链路 sender 防护在 72947d9（T26） |
| M-16 | 已修复 | 1a844ab | requestSingleInstanceLock 单实例锁（T3） |
| M-17 | 已修复 | 507b0a0 | message:getByGroupId 分页加载（T27） |
| M-18 | 已修复 | 63c9e42 | reorder 批量 position 更新包事务（T28） |
| M-19 | 已修复 | 506fd29 | 末尾用户消息无条件追加（T24） |
| M-20 | 已修复 | 506fd29 | 指令先校验后写库 + 角色查询加 group_id 条件（T24） |
| M-21 | 已修复 | 0cdd9c0 | 字符预算截断（24000 字符，裁剪顺序：历史→记忆→背景；附测试）（T25） |
| M-22 | 已修复 | a5eba53 | escapeLike + ESCAPE 子句；角色查询加 LIMIT（T10） |
| M-23 | 已修复 | 63c9e42 | 修正误导注释并注明 FTS5/worker 为后续方向；FTS5 本体为遗留项（T28） |
| M-24 | 已修复 | 5d30625 | 余波查询 LEFT JOIN characters 取真实角色名（T30） |
| M-25 | 已修复 | 5d30625 | escapeRegExp 抽共享工具后拼正则（T30） |
| M-26 | 已修复 | 5d30625 | @提及捕获剥离标点后再匹配（T30） |
| M-27 | 已修复 | 5d30625 | "平静"事件写 intensity 0，不再走 0.6 强度（T30） |
| M-28 | 已修复 | 5d30625 | updateFromLLM 情绪词白名单 + Number.isFinite（T30） |
| M-29 | 已修复 | 50bca16 | logger 内存字节计数，超限才 statSync 校验（T29） |
| M-30 | 已修复 | 466e3fb | narrative store 写操作统一走 useApi.call（T33） |
| M-31 | 已修复 | 9742a79 | 标签/对话框/Toast 键盘访问与 ARIA 最小集；focus trap 完整实现为遗留项（T34） |
| M-32 | 已修复 | 2b623c4 | EmotionTag 监听器移至 await 之前（T21） |
| M-33 | 已修复 | 2b623c4 | 事件卡片 disabled + sending 检查 + try/catch（T21） |
| M-34 | 已修复 | 2b623c4 | 气泡视图对流式消息的编辑/删除/重发防护（T21） |
| M-35 | 已修复 | 588088d | 五个对话框统一 submitting 锁 + disabled（T18） |
| M-36 | 已修复 | 466e3fb | GroupSettingsDialog 模板 v-if="group" 防护（T33） |

## 低优先级问题（主进程，逐行对照报告 §6）

| 位置（报告 §6 行） | 状态 | 修复 commit | 备注 |
|---|---|---|---|
| llm-profiles.js:113-117（id 展开顺序） | 已修复 | 28183be | `{ ...profile, id: generateUUID(), createdAt }`（T12） |
| llm-profiles.js:39-43,73-76（数组校验/迁移入队/日志文案） | 已修复 | 28183be | Array.isArray 回退 []；迁移写回走 enqueueWrite；文案与实际一致（T12） |
| system-prompts.js:123（Date.now 模板 id） | 已修复 | 832e8d5 | 改用 generateUUID（T11） |
| global-character-manager.js:288-291（deleteTag 冗余 DELETE） | 已修复 | a5eba53 | 简化为级联删除（T10） |
| manager.js:15,312-318（LRU 上限 10） | 已修复 | 28183be | 上限提至 64（T12） |
| manager.js:403-406（附属文件不清理） | 已修复 | 28183be | 循环清理 ''/-journal/-wal/-shm（T12） |
| config-dir.js（重复实现） | 已修复 | 28183be | ensureConfigDir 委托 ensureDataDir（T12） |
| channels.js（死代码） | 已修复 | 28183be | 整文件删除（T12） |
| group.js:39（use_global_api_key 布尔归一） | 已修复 | 832e8d5 | boolFrom 辅助统一归一 0/1（T11） |
| config.js:40-78,125-128（save 无 error 字段） | 已修复 | 832e8d5 | wrapSaveResult 统一带 error（T11） |
| scripts/dev.js:26-28（信号终止误报成功） | 已修复 | 28183be | `code ?? 1`（T12） |
| base-client.js:153-158（abort 监听器累积） | 已修复 | 3302a8c | 请求结束后移除监听器（T15） |
| client.js:68-70、anthropic-client.js:88-90（data: 前缀容错） | 已修复 | c12e2f4 | 兼容无空格写法（T14） |
| client.js:178-183（纯思考模型空 content 判失败） | 已修复 | 6a4807c | reasoning_content 有值时放行（T16） |
| proxy.js/client.js/preload 死代码群（getModels 等） | 已修复 | 0e353ab | 删除 getModels 死链路、buildAxiosProxyConfig、前端代理 store；主进程 handler 留作旧数据兼容（T17） |
| profileId 静默回退 | 已修复 | 0e353ab | 显式传入不存在时报错而非回退第一个（T17） |
| llm.js:301,331（replace '{hint}' $ 模式） | 已修复 | 0e353ab | 改函数替换（T17） |
| llm-response-handler/llm.js（sender.send 无 isDestroyed） | 已修复 | 72947d9 | safeSend 封装 5 处直发点（T23） |
| memory.js:15-19（memory:add 无校验） | 已修复 | 63c9e42 | 非空字符串校验（T28） |
| global-character.js:75-78,273-279（null.trim） | 已修复 | 63c9e42 | null 防护（T28） |
| global-character.js:126-159（importToGroup 按名字防重） | 已修复 | 63c9e42 | 改按 id 防重复用库 ID（T28） |
| narrative.js 等 12 处（_getGroupDB 封装泄漏） | 已修复 | 63c9e42 | engine 暴露公开 getGroupDB（T28） |
| uuid.js:4-9（Math.random UUID） | 已修复 | 4750bd4 | 改 crypto.randomUUID（T31） |
| engine.js:53 + emotion-manager.js:159-162（死参数） | 已修复 | 5d30625 | senderCharacterId 接线实现负好感深度推断（T30） |
| event-trigger.js:156（sort 洗牌） | 已修复 | 5d30625 | 改 Fisher-Yates 副本洗牌（T30） |
| event-trigger.js:141-185（prepare vs prepareCached） | 已修复 | 5d30625 | 5 处统一 prepareCached（T30） |
| prompt-builder.js:52-56（IN () 非法 SQL） | 已修复 | 5d30625 | 空角色列表防护（T30） |
| constants.js:12,19（词表重复/误判） | 已修复 | 5d30625 | "算了"归无奈；删"不是"/裸"为什么"（T30） |
| relationship-manager.js:117（负向 floor 不对称） | 已修复 | 5d30625 | 改 Math.trunc（T30） |
| engine.js:229-234（情绪 N+1） | 已修复 | 5d30625 | 改 getEmotionsBatch 批量查询（T30） |

## 低优先级问题（前端，逐行对照报告 §6）

| 位置（报告 §6 行） | 状态 | 修复 commit | 备注 |
|---|---|---|---|
| stores/groups.js:37-41（deleteGroup 回退） | 已修复 | be6e3a8 | 回退到第一个群组（T22） |
| composables/useApi.js（并发 loading 提前复位） | 已修复 | be6e3a8 | loadingCount 计数器（T22） |
| composables/useDialog.js（离场动画/冗余 Promise） | 已修复 | 1ab2f7e | 延迟 removeChild 300ms + 删冗余 Promise（T36） |
| common/FormGroup.vue:7（死分支） | 已修复 | 1ab2f7e | 删除（T36） |
| common/ z-index 硬编码 | 已修复 | 1ab2f7e | 抽 $z-index-* token（T36） |
| utils/validators.js:7-11（非字符串防御） | 已修复 | be6e3a8 | 入口 String 化（T22） |
| ChatWindow.vue:541-543（高亮 setTimeout） | 已修复 | 25d6361 | 句柄保存 + 卸载清理（T20） |
| ChatWindow.vue:551-561（streamingTick/rAF） | 已修复 | 25d6361 | displayMode 区分 + rAF 卸载取消（T20） |
| CharacterPanel.vue:221-235,683-702（v-model 写 store/失败恢复覆盖） | 已修复 | 1ab2f7e | command 本地草稿化（commandDrafts Map）（T36） |
| CharacterPanel.vue:17-27,575-583（maxHistory NaN） | 已修复 | 1ab2f7e | 1-50 校验 + 回显当前值（T36） |
| MessageInput.vue:59-64（失败不回填） | 已修复 | 1ab2f7e | restore() 暴露 + ChatWindow 失败回填（T36） |
| RelationshipPanel.vue:105-117（静默失败/删除无确认） | 已修复 | 466e3fb + 1ab2f7e | toast 封装（T33）+ 校验提示与删除确认（T36） |
| GroupSearch.vue:62,72-84（防抖/请求序号） | 已修复 | 9032acf | onUnmounted 清理 + searchSeq 守卫（T2） |
| CharacterGachaDialog.vue:184-186（canGenerate 恒真） | 已修复 | 25cce92 | 删除死代码（T35） |
| CharacterLibrary.vue:190-199,218-220（分页/判重） | 已修复 | 25cce92 | 页码校正 + 按 id 判重（T35） |
| QuickGroupDialog.vue:107-109,476-484（index key/统计/孤儿） | 已修复 | 25cce92 | _key 稳定 key + 统计口径 + 失败回滚（T35） |
| QuickGroupDialog/GlobalCharacterDialog（年龄仅 HTML 校验） | 已修复 | 1ab2f7e | JS 校验 1-999 整数（T36） |
| LLMConfigPanel.vue:88（遮罩关闭丢 Key） | 已修复 | b3aa197 | 遮罩关闭禁用（T32） |
| CreateGroupDialog.vue:180-187（模板重复叠加） | 已修复 | 25cce92 | 应用后清空选中（T35） |
| GroupSettingsDialog.vue:101,173-194（空群名） | 已修复 | 466e3fb | 保存前非空校验（T33） |
| App.vue（100vw 溢出） | 已修复 | be6e3a8 | 改 100%（T22） |

## §7 重复代码与架构问题

| 项目 | 状态 | 修复 commit | 备注 |
|---|---|---|---|
| CharacterPanel.vue 上帝组件拆分 | 遗留 | — | 遗留-P3（规格 §4 排除，择机重构） |
| 对话框双实现/整块复制（usePromptConfig 抽取、统一对话框体系） | 遗留 | — | 遗留-P3 |
| getProviderName 6 份等工具函数收敛 | 遗留 | — | 遗留-P3 |
| channels.js 形同虚设 | 已修复 | 28183be | 死文件已删除（T12） |

## 遗留项汇总（P3 / 规格 §4 排除）

1. **P3 架构重构**：拆分 CharacterPanel.vue、统一两套对话框体系、抽取 usePromptConfig、getProviderName 收敛（§7 第 1/2/3 条）。
2. **FTS5 / worker 搜索**（M-23 本体）：本次仅修正注释并注明方向，未引入 FTS5。
3. **记忆 schema 改 character_id 关联**（M-4 完整方案）：本次用"改名迁移记忆"最小方案替代。
4. **focus trap 完整实现**（M-31）：本次仅做键盘访问与 ARIA 最小集。

## 复查不成立项

无（计划执行中全部复查成立，无驳回项）。
